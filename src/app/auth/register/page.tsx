import { RegisterForm } from '@/features/auth/components/RegisterForm'

/**
 * Renders the registration page by returning the registration form component.
 *
 * @returns The JSX element that renders the RegisterForm for user sign-up.
 */
export default function RegisterPage() {
  return <RegisterForm />
}
